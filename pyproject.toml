[project]
name = "pp-doclayout-onnx-tools"
version = "0.1.0"
description = "Python helper scripts for PP-DocLayoutV3 ONNX assets."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "huggingface-hub>=0.24.0",
]

[tool.uv]
package = false
